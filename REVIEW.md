# 发布前代码审查与验收

审查日期：2026-09-19。项目定位：JRiver 桌面小窗；歌词是常用功能，曲评是可选附加项。

## 已处理

1. **个人环境耦合**：未复制私人曲评数据库适配器、NAS 地址、凭据或整套中控台。连接信息通过 `config.json` 配置；未配置曲评文件时隐藏整个曲评区域。
2. **窗口空间**：关闭曲评附加功能时，窗口缩小；向下拉伸扩展歌词区，不留空白曲评框。
3. **固定中控台地址**：只在用户配置 HTTP(S) 地址后显示入口。
4. **跨主机误写星级**：COM 评分写入仅允许本机 Windows MCWS，并校验 FileKey、完整路径和原星级；远程 MCWS 不调用本机 COM。
5. **网络与数据边界**：自动云歌词匹配默认关闭。可选曲评不包含生成器或模型密钥；输入错误、重复录音路径和非法分数不破坏播放控制。
6. **数据与实例隔离**：配置目录和单实例名称与个人旧版分开；不覆盖旧卡片的窗口位置、歌词缓存或启动设置。

## 已验证

- 15 项本地单元测试：歌词身份、时间轴、播放指令范围、评分写入及撤销保护、可选曲评解析与更新。均使用模拟服务／临时文件，不写真实曲库。
- Windows 原生窗口打开、关闭及快捷键释放。
- 3 种视图 × 4 种缩放（50%、75%、100%、125%）；最小迷你窗为 230 × 97 逻辑像素，已经检查截图。
- 实际鼠标点击：可操作模式阻挡后方测试窗口；穿透模式让后方独立进程收到点击；快捷键返回可操作模式后恢复阻挡。
- 主快捷键被现用卡片占用时，Ctrl+Alt+Shift+F10 回退注册并实际切换成功。
- 托盘菜单切换、隐藏与恢复；无曲评模式下拉长窗口会增加歌词空间。
- 新版 Android 的真实 JRiver 播放列表切换另在私人项目完成验收，不计作本桌面项目的跨平台验证。

可复跑：`python -m unittest -v test_floating test_cloud_lyrics test_reviews`；
`python qa_window.py` 会短暂打开两个独立测试窗口，使用虚构歌曲检查真实窗口点击与快捷键，不连接 JRiver。
证据写入被 Git 忽略的 `.qa/`。README 截图仅含虚构元数据和自行绘制的图标。

## 仍需如实说明的兼容性范围

- 当前正式验收环境是 Windows 11 / JRiver 36。Windows 10、其他 JRiver 版本、HTTP 认证组合、非默认端口需由相应环境继续验证。
- 鼠标穿透、全局快捷键、评分 COM 使用 Windows API；本版本不声称兼容 macOS/Linux。
- 多显示器不同 DPI、任务栏位置和各类游戏的独占全屏行为不能仅凭本机窗口测试保证。
- 云歌词提供方的可用性与接口稳定性不由本项目控制。
- 没有发现应阻止当前源码候选交付的凭据泄露；这不是覆盖全部环境的安全认证。
- Android Tempus 衍生版保留其上游 GPL-3.0 约束，不放进本 MIT 桌面源码包。

当前发布的是可审阅的源码候选，不包含 JRiver 程序、私人曲库或预打包运行环境。
